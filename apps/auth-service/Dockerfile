# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @ecartecommerce/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t ecartecommerce-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system ecartecommerce-auth-service && \
          adduser --system -G ecartecommerce-auth-service ecartecommerce-auth-service

COPY apps/auth-service/dist ecartecommerce-auth-service/
COPY apps/auth-service/package.json ecartecommerce-auth-service/
RUN chown -R ecartecommerce-auth-service:ecartecommerce-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix ecartecommerce-auth-service --omit=dev -f install

CMD [ "node", "ecartecommerce-auth-service" ]
